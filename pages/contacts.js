async function render(data = {}, opts = {}) {
  return `<section aria-label="Контакты">
    <h1>Контакты</h1>
    <p>Свяжитесь с нами любым удобным способом.</p>
  </section>`;
}

module.exports = { render };